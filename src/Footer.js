//Footer that displays in the message board application
import React from 'react';

export const Footer = ({className}) => (
    <div className={className}>
        <p className="text-right" >&copy;2020 Jennifer Tang-Tran</p>
    </div>
)

export default Footer;